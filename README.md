# Guitar Learning App 🎸

一個專為電吉他學習者打造的全端學習紀錄平台。提供從基礎到進階的技術知識樹、樂理教學，並整合了互動式指板工具與學習進度追蹤功能。

## 🌟 核心功能

- **Markdown 為底的知識庫**：所有教材皆透過 Markdown 撰寫，後端會自動掃描產生教學目錄與文章結構。
- **知識圖譜與技能樹**：視覺化呈現各項彈奏技巧與樂理知識的關聯性。
- **互動式指板 (Interactive Fretboard)**：支援點擊標記與音名懸浮預覽，方便輔助教學與理解音階。
- **進度追蹤與打卡熱力圖**：紀錄每篇文章的完成度，並透過 GitHub 風格的熱力圖呈現每日練習狀態。
- **反向連結關聯**：自動解析 Markdown 內鏈 (WikiLink 格式或標準 Markdown)，在文章底部推薦相關閱讀。
- **深色模式 UI**：採用 Tailwind CSS + Glassmorphism 打造具現代感的暗色佈景。

## 🛠 技術堆疊

- **前端**：React 18 + Vite, Tailwind CSS, React-Markdown, React Router, Recharts, D3 (知識圖譜)
- **後端**：Node.js + Express, TypeScript
- **資料庫**：MongoDB (負責存儲使用者進度、打卡紀錄與成就)
- **部署**：Docker / Docker Compose (Nginx Proxy)

---

## 💻 快速開始 (本地環境)

請確保您已安裝 [Node.js](https://nodejs.org/), [PNPM](https://pnpm.io/), 以及 [Docker](https://www.docker.com/)。

1. **啟動本地資料庫**
   建議透過 Docker 啟動一個本地的 MongoDB：

   ```bash
   docker run -d -p 27017:27017 --name guitar-mongo mongo
   ```

2. **安裝所有依賴**
   專案根目錄執行：

   ```bash
   pnpm install
   ```

3. **啟動後端服務** (預設執行於 Port 5000)

   ```bash
   cd backend
   pnpm dev
   ```

4. **啟動前端服務** (預設執行於 Port 5173)
   ```bash
   cd frontend
   pnpm dev
   ```

👉 前往 http://localhost:5173 即可開始瀏覽。

---

## 🚢 生產環境部署 (Portainer / Docker Compose)

本專案已完美支援 Docker 化部署，特別適合透過 Portainer 進行管理。前端內建 Nginx 作為反向代理，後端則安全地隱藏在 Docker 內部網路中。

### Portainer 部署步驟：

1. 在 Portainer 的 **Stacks** 中選擇 **Add stack**。
2. 選擇 **Repository** 模式，貼上本專案的 GitHub Repo 網址。
3. **Compose path** 填入根目錄的 `docker-compose.yml`。
4. **Environment variables** (環境變數設定)：
   如果您伺服器母機有直連的 MongoDB，請直接設定：
   - Name: `MONGODB_URI`
   - Value: `mongodb://[帳號]:[密碼]@host.docker.internal:27017/guitar-learning?authSource=admin`
5. 點擊 **Deploy the stack**，Nginx 首頁將會開放在伺服器的 **3000** 通訊埠 (`http://您的伺服器IP:3000`)。

> **資安提示**：
> `docker-compose.yml` 已預設關閉 `backend` 服務的對外 `ports` (Port 5000)，所有的 API 請求 (`/api/*`) 皆會先通過前端 Nginx (Port 80) 再由內部網路轉發給後端，確保了高度的隔離與安全性。

---

## 📂 專案結構

```
guitar-learning-app/
├── backend/                  # Express 伺服器
│   ├── src/content           # 📝 Markdown 教材知識庫 (在此新增/修改文章)
│   ├── src/models            # Mongoose Schema
│   ├── src/routes            # API 路由
│   └── src/services          # 核心商業邏輯 (如: Markdown 連結解析)
├── frontend/                 # React 客戶端
│   ├── src/components        # 介面元件 (含互動式指板)
│   ├── src/pages             # 頁面 (HomePage, ArticlePage)
│   ├── index.css             # Tailwind 全局樣式
│   └── nginx.conf            # 生產環境 Nginx Reverse Proxy 設定
└── docker-compose.yml        # 生產環境一鍵部署配置
```
